1. Clone Repo into your terminal using git clone <url>
2. Run code using sh run.sh terminal command and make sure to run in the Ensemble Learning directory
3. Code will display slice of the error rates from the tested 500 trees, default testing function is bagged() since no other 
    function was confirmed to be working or implemented.